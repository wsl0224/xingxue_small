var publicFun={
  // 根据状态num显示状态中文
  showStatusLabel: function (e) {
    if (e == 1) {
      return '待选择';
    }
    if(e==2){
      return '待服务'
    }
  },
  // 根据状态num显示图片
  showStatusImg:function(e){
    if(e==1){
      
      return '../image/icon/icon56.png';
    }
  }
}

module.exports = {
  showStatusLabel: publicFun.showStatusLabel,
  showStatusImg: publicFun.showStatusImg,
}
